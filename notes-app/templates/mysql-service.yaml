apiVersion: v1
kind: Service

metadata:
  name: db

spec:
  selector:
    app: {{ .Release.Name }}-mysql

  ports:
    - port: {{ .Values.mysql.service.port }}
      targetPort: 3306

  type: ClusterIP
